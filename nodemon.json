{
  "ignore": [
    "**/*.test.ts",
    "**/*.spec.ts",
    ".git",
    "node_modules"
  ],
  "watch": ["src"],
  "exec": "node --inspect=0.0.0.0:9229 --enable-source-maps -r tsconfig-paths/register -r ts-node/register ./src/index.ts",
  "ext": "ts, js",
  "scripts": {
    "dev": "nodemon"
  }
}
